# 출시 체크리스트

감사일 2026-08-30 기준. 체크되지 않은 항목은 아직 통과하지 못한 것입니다.

## 게이트 0 — 데이터 정직성 (하나라도 실패하면 출시 금지)

- [x] 생성기가 자기 출력에 `humanVerified` / `reviewed` 를 찍지 않는다 (P0-1)
- [x] 실제 뉴스 레코드의 `publishedAt` 이 원문과 일치한다 (P0-2)
- [x] 실제 뉴스 레코드의 `publishedAt` 이 `00:00:00Z` 가 아니다 (검증기가 차단)
- [x] 데모 레코드가 `isDemo: true` 로 표시된다
- [x] 데모 레코드에 UI 배지가 목록·상세 양쪽에 붙는다
- [x] `validate_data.py public-data --production` 이 데모 포함 시 실패한다 (exit 1)
- [x] 기사 본문·언론사 이미지를 저장하지 않는다
- [x] 야구여왕2 회차 결과를 임의 작성하지 않았다
- [x] D+11 이후 일별 예보를 생성하지 않는다 (자동 테스트)

## 게이트 0.5 — 감사 수정분 (2026-08-30 추가)

- [x] 팀 순위를 앱이 계산하고 규정을 화면에 밝힌다 (P1-5)
- [x] `spoilerLevel` 미지정 콘텐츠가 fail-open 하지 않는다 (P1-6)
- [x] 글자 배율 200%까지 지원하고 360dp에서 넘치지 않는다 (P1-7)
- [x] 알림 payload → 화면 매핑이 존재하고 테스트된다 (P1-2, 실기기 확인은 게이트 2)
- [x] 경기 상태 전 종류(live·cancelled·delayed·forfeit)가 fixture에 있다 (P2-5)
- [x] 파이썬 파이프라인 테스트가 CI에서 돈다 (P2-4)
- [x] 터치 영역이 실제 렌더 크기로 검증된다 (P2-7)
- [x] 골든이 결정론적이다 (시계 고정)
- [ ] 발견 탭에 실제 콘텐츠가 있다 (P1-4 — API 키 필요)

## 게이트 1 — 빌드와 정적 검사

- [x] `flutter pub get`
- [x] `dart format --output=none --set-exit-if-changed .`
- [x] `flutter analyze` 이슈 0
- [x] `flutter test` 전부 통과 (308개) + 파이썬 22개
- [x] `flutter build apk --debug`
- [x] `flutter build apk --release`
- [x] 저장소에 API 키·토큰·절대경로 없음
- [x] `dart_test.yaml` 에 사용 중인 태그 선언 (P2-2)
- [x] README의 APK 크기 등 수치가 실제와 일치 (P2-3)

## 게이트 2 — 런타임 (현재 전부 미수행)

**이 게이트는 한 번도 실행된 적이 없습니다.** 에뮬레이터 또는 실기기가 필요합니다.

- [x] 에뮬레이터 또는 실기기에서 앱이 시작된다 (API 35)
- [x] 첫 실행과 재실행이 모두 정상
- [x] 기내 모드 최초 실행에서 빈 화면이 아니다
- [x] 동기화 후 네트워크를 끊어도 마지막 데이터가 유지된다
- [x] 알림 권한을 거부해도 앱이 정상 동작한다
- [x] 위치 권한을 요청하지 않는 것을 실제로 확인 (매니페스트에 없음)
- [x] 캘린더 핸드오프가 실제 캘린더 앱을 연다
- [x] 길찾기가 실제 지도 앱을 연다
- [x] 공유 시트가 열린다
- [x] Android 뒤로가기가 예상대로 동작 (제스처 네비게이션은 미검증)
- [x] 알림이 예약 시각에 도착한다 (시계를 앞당겨 확인)
- [x] **알림을 누르면 해당 경기 상세가 열린다** (P1-2)
- [x] 재부팅 후 예약된 알림이 복구된다
- [x] 시간대를 바꾼 뒤에도 예약이 어긋나지 않는다
- [x] TalkBack 탐색 순서와 라벨 확인 (데모 표시 누락 발견·수정)
- [x] 시스템 글자 크기 최대(200%)에서 핵심 정보가 잘리지 않는다 (P1-7)

## 게이트 3 — 핵심 과업

- [x] 현역: 홈 → 다음 경기 상세 1탭
- [x] 통합 검색 어느 탭에서나 1탭
- [x] 위치 권한 없이 근처 경기 2탭
- [x] 탭 전환 후 필터·날짜 복원
- [x] **신규 설치 딥링크가 목적지를 연다** (P1-1, 회귀 테스트 있음)
- [ ] **공식 원문을 확인할 수 있는 경기가 존재한다** (P1-3)
- [x] 공유 링크가 앱 미설치자에게도 유효하다 (P1-3, 죽은 스킴 폴백 제거)
- [ ] 방송 콘텐츠에서 실제 경기·팀으로 이어지는 실제 데이터 경로 존재 (P1-4)

## 게이트 4 — 스토어 제출물

- [x] **앱 아이콘이 Flutter 기본 아이콘이 아니다** (`tools/make_launcher_icon.py`)
- [ ] 릴리스 서명 키 생성, 키스토어 안전 백업
- [x] `flutter build appbundle --release` 성공 (62.6MB)
- [ ] `WB_MANIFEST_BASE_URL` 을 실제 배포 주소로 지정해 빌드
- [ ] 개인정보처리방침 URL 준비
- [ ] Play Console 데이터 보안 섹션 작성 (수집 없음 / 위치 미사용)
- [ ] **스토어 설명에 데모 데이터임을 명시**
- [ ] 스크린샷이 데모 배지가 보이는 상태로 촬영됨

## 게이트 5 — 운영 준비

- [x] 게시 워크플로가 검증 실패 시 롤백한다
- [x] 한 원천 실패가 전체 배포를 막지 않는다
- [x] WBAK·KBSA가 이중 잠금으로 비활성
- [ ] Google Forms 5종 생성 및 URL 주입
- [ ] 개인정보 삭제 요청 창구 운영자 지정
- [ ] 뉴스 검수 담당자와 승격 절차 1회 리허설
