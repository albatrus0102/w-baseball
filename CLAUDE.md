# w-baseball

한국 여자야구(女子野球) 정보 앱. Flutter, 안드로이드 우선. 광고도 유료 기능도
계정도 없습니다. 모든 데이터는 기기 안에만 저장됩니다.

**주 사용자는 선수입니다.** 동호인 여자야구를 실제로 하는 사람이 자기 팀 일정,
경기 결과, 자기 기록을 보러 옵니다. 관중용 앱이 아닙니다.

사용자와는 **한국어로** 대화합니다.

---

## 이 앱의 성격 — 이걸 모르면 전부 어긋납니다

**국내 여자야구는 공개 데이터가 없습니다.** 연맹의 공개 API가 확인되지 않았고,
공공데이터포털에 `여자야구`로 검색하면 0건입니다. 그래서 지금 앱에 보이는 팀·
경기·선수는 **거의 전부 데모 픽스처**이며 `isDemo: true` 로 표시되어 화면에도
"데모" 라고 나옵니다.

이 앱이 지켜 온 선은 하나입니다: **없는 것을 있는 것처럼 보이게 하지 않는다.**
신선도를 부풀리지 않고, 데모를 실제처럼 위장하지 않고, 출처 없는 기록을 만들지
않습니다. 화면 문구 하나까지 이 원칙으로 쓰여 있습니다.

---

## 🚫 절대 하지 말 것

되돌릴 수 없거나, 남에게 피해가 가거나, 앱의 존재 이유를 무너뜨리는 것들입니다.

- **API 키·서비스 계정·토큰을 코드나 저장소에 넣지 않습니다.** `--dart-define`
  또는 GitHub Secrets 로만. 예외 없습니다.
- **서명 키를 커밋하지 않습니다.** 분실하면 스토어 업데이트가 영구 불가입니다.
- **WBAK·KBSA 의 비공개 내부 주소를 공식 API로 가정하지 않습니다.**
- **기술적으로 된다는 이유로 웹 스크래핑을 켜지 않습니다.** 이용허락이 필요한
  어댑터는 기본 비활성이고, `scripts/ingest/fetch_sources.py` 의
  `blocked_reason` 이 하드코딩으로 두 번째 잠금을 겁니다. **허락 없이 그 줄을
  지우지 마세요.** 절차는 `docs/data-sources.md` 의 "켜는 절차" 에 있고,
  1번(서면 허락)보다 3번을 먼저 하면 안 됩니다.
- **선수의 전화번호·주소·개인 이메일·정확한 생년월일을 수집하거나 표시하지
  않습니다.**
- **미성년 선수의 사진과 개인 프로필은 명시적 사용 근거 없이 표시하지 않습니다.**
- **샘플 데이터를 실제 공식 기록처럼 위장하지 않습니다.** 모든 샘플은
  `isDemo=true` 또는 명확한 데모 라벨을 답니다.
- **출처가 확인되지 않은 선수 정보나 경기 결과를 만들어내지 않습니다.**
  데이터가 없다는 이유로 그럴듯한 방송 결과·선수 기록·30일 일별 날씨를
  지어내면 안 됩니다.
- **기사 본문과 언론사 이미지를 복제하지 않습니다.** 제목·매체·시각·링크만.
- **외부 분석 SDK는 기본 비활성**이며 동의와 방침 없이 켜지 않습니다.
- **가짜 URL을 넣지 않습니다.**

감사 중 개인정보 노출·비밀키 포함·저작권 침해 가능성·실제 데이터 오표시를
발견하면 **선택 사항으로 축소하지 말고 즉시 출시 차단 문제로 보고**하세요.

---

## 커밋 전 게이트

```bash
python tools/commit_gate.py
```

12단계, 약 60~90초. **12/12 통과해야 커밋합니다.** Flutter 는 `PATH` →
`FLUTTER_ROOT`/`FLUTTER_HOME` → gitignore 된 `.flutter_root` 순으로 찾습니다.

**Flutter 가 없는 환경(클라우드 세션 등)에서는 게이트가 안 돕니다.** 그럴 때는
**GitHub Actions CI 가 검증을 맡습니다** — `.github/workflows/ci.yml` 이 로컬
게이트 12단계를 전부(선언된 asset git 추적 확인·색 대비 래칫 포함) 같은 방식으로
돌리고, 거기에 로컬 게이트가 일부러 빼는 Android APK 빌드가 하나 더 있습니다
(release-artefact 빌드일 뿐, `flutter analyze`/`flutter test` 가 이미 검사하지
않는 것을 증명하지 않아서입니다). 게이트를 못 돌렸으면 **"통과"라고 쓰지 말고
"돌리지 못했고 CI 결과를 기다린다"** 고 쓰세요. 못 돌린 것과 돌려서 통과한 것은
다릅니다.

---

## 이 저장소에서 사람들이 반복해서 데인 자리

전부 실제로 한 번 이상 사고가 난 곳입니다.

- **`dart format` 은 기본이 파일을 고쳐 씁니다.** 검사만 하려면
  `--output=none --set-exit-if-changed`. **서식 검사가 실패하면 그 파일이 내
  것인지 `git status` 로 먼저 확인하세요** — 남의 작업을 재포맷한 사고가 두 번
  났습니다.
- **지연 배치를 조심하세요.** `ListView` 와 `CustomScrollView` 는 화면 밖
  위젯을 **build 조차 하지 않습니다.** 스크롤 없이 검사하면 아무것도 안
  나오는데, 그건 통과가 아니라 **미측정**입니다. `test/widget/harness.dart` 의
  `scrollToEnd` 를 `expectNoOverflow` 앞에 부르세요. 이 함정 때문에 기본 글자
  크기에서 33px 넘치던 화면이 오래 방치됐습니다.
- **골든은 코드만의 함수여야 합니다.** 시계를 고정하지 않으면 "방금 확인" 이
  "5시간 전 확인" 으로 바뀌어 코드를 안 고쳐도 골든이 깨집니다.
  `buildTestApp(frozenNow: ...)` 를 쓰세요. `lib/features` 안에서
  `DateTime.now()` 를 직접 부르면 `test/audit/source_rules_test.dart` 가
  잡습니다 — `clockProvider` 를 쓰세요.
- **화면을 바꿨으면 골든을 이미지로 직접 열어 보세요.** 갱신만 하고 안 보면
  의도치 않은 변화가 그대로 들어갑니다.
- **KST 는 UTC 로 저장하고 표시할 때만 서울로 바꿉니다.** `Kst.todayKst` 는
  기기 표준시의 영향을 받지 않도록 **UTC 맛** `DateTime` 을 돌려줍니다.
- **`WbCard` 의 `semanticLabel` 은 하위 트리를 `ExcludeSemantics` 로 감쌉니다.**
  안쪽에 `Semantics` 를 넣어도 조용히 삼켜집니다.
- **`onTap: () {}` 은 "아무것도 안 함" 이 아닙니다.** null 이 아닌 `onTap` 은
  물결 효과를 그려서 눌리는 것처럼 보이게 만듭니다 — 거짓 어포던스입니다.
- **`Row(Expanded(Text), 버튼)` 은 절대 넘치지 않습니다.** `Expanded` 가 압박을
  흡수하니까요. 대신 큰 글자에서 문장이 좁아져 낱말 가운데가 끊깁니다. 넘침
  검사로는 안 보입니다. `WbNoticeWithAction` 을 쓰세요.
  `scripts/validate/find_squeezed_rows.py` 가 남은 자리를 셉니다.
- **git 은 빈 디렉터리를 추적하지 않습니다.** `pubspec.yaml` 에 선언만 하고
  파일이 없는 asset 디렉터리는 새로 받은 저장소에서 `flutter analyze` 를
  깨뜨립니다.
- **공유 파일은 통째로 복사하지 말고 패치로 얹으세요.** 워크트리나 다른
  세션의 작업을 가져올 때, 두 쪽이 같은 파일을 만졌으면 복사가 한쪽을
  조용히 지웁니다. **덮인 코드는 어떤 테스트도 실패시키지 않습니다** —
  게이트도 CI도 사라진 코드는 못 봅니다. `git diff` 로 패치를 떠서
  `git apply --check` 로 확인하고 붙이세요. 실제로 겹치는 자리:
  `test/screenshots/capture_test.dart`, `test/widget/harness.dart`.
- **파이프에 물린 종료코드를 읽지 마세요.** `cmd | tail` 의 `$?` 는 `tail` 의
  것이고, `cmd; echo "$?"` 를 백그라운드로 돌리면 `echo` 의 것입니다. 이
  프로젝트에서 네 번 틀렸습니다. **파일에 적어서 읽으세요.**

---

## 작업 방식

사용자가 다른 앱을 여러 달 만들며 실제로 데이고 확립한 규율입니다. 각 항목이
한 번씩 크게 데인 자리입니다.

### 역할 분담

| | 누가 | 무엇 |
| --- | --- | --- |
| 설계 | FABLE | 방향·설계·시안 **3~4안**과 추천 |
| 구축 | Sonnet | 만들기. **설계 문제를 먼저 말할 권한이 있음** |
| 감리 | Opus (주 세션) | 측정 지원(결정 안 함)·인수검수·전체검수 |

🚩 **감리자는 만들지 않습니다.** 잔손질까지 구축자에게 넘깁니다. 감리자가 손댄
자리는 아무도 검수하지 않는 유일한 사각지대가 됩니다.

🚩 **만든 이가 자기 것을 검증하면 같은 맹점을 두 번 통과합니다.**

**감리는 두 시점, 방법은 반드시 다르게.** 성과는 독립적인 두 방법이 어긋나는
칸에서 나옵니다.

- **인수검수**(직후): 시킨 대로 됐나 — 게이트 전부, 고치기 전과 **양방향 대조**,
  변이 검증, 형제 경로 전수
- **전체검수**(릴리스 전): 전제가 맞나, 실제로 그렇게 보이나 — 물려받은 상수·
  전제 재검증, 수치 측정, 실데이터 부팅
- 🚩 **전체검수를 `git diff` 로 좁히지 마세요.** 원인이 안 바뀐 코드에 있는 경우가
  흔합니다.

### 결함을 만났을 때

**① 현황 → ② 재현 → ③ 원인 확정(되돌리면 사라지는지) → ④ 수정 → ⑤ 재확인 →
⑥ 회귀 테스트**

🚩 **②를 하기 전에는 "원인을 찾았습니다" 라고 쓰지 마세요.** 보고 문장 자체를
막아야 지켜집니다. `grep` 한 줄로 구조를 단정하는 것이 최다 오진입니다.
(이 저장소에서도 라우터 결함을 구조만 보고 단정했다가, 재현해 보니 음성이었던
일이 있습니다.)

### 재는 것 자체를 의심

- 도구부터 **대조군과 함께** 검증하세요
- "우연히 같을 리 없는 값이 같다" 는 고장 신호입니다
- **주석은 그때의 의도이지 지금의 사실이 아닙니다.** 이 저장소에서 주석이
  코드와 어긋난 사례가 여럿 나왔습니다 — 상한을 2.0 으로 올린 근거가
  "1.3/1.4/1.7/2.0 에서 확인했다" 였는데 실제 단언은 1.4 한 화면뿐이었습니다
- **분모와 필드 경로를 의심하세요** — 한 단계 위를 세면 항상 0 입니다
- 표본이 작으면 비율로 강한 결론을 내지 마세요
- 테스트가 실패하면 **낡은 테스트나 내 테스트가 틀렸을 가능성부터** 보세요

### 변이 검증

되돌린 사본에서 **정말 실패하는지** 확인하세요. 단위 테스트는 함수 동작만
보증하고 **호출부가 그걸 부르는지는 보증하지 못합니다.**

변이만으로는 부족합니다 — "안 고쳐야 할 것이 안 걸리는가" 는 **고치기 전 사본과
양방향 대조**로만 알 수 있습니다.

### 90초 규칙

🚩 넘으면 멈추고 보고·원인 분석·다른 일을 하세요. **조용히 기다리는 선택지는
없습니다.** 최악은 폴링 루프입니다 — 백그라운드 작업은 끝나면 알림이 옵니다.

### 보고

- **어떤 모델이 무엇을 했는지 항상 밝히세요.** 주 세션이 직접 한 것도 구분해서.
  커밋 메시지에도 한 줄.
- **고친 것·안 고친 것·이유를 전부 쓰세요. 조용히 빼지 않습니다.**
- **못 잡는 것을 잡은 척하지 마세요.** 실제 터치 제스처, 소프트키보드 겹침,
  실기기 성능은 **"미확인"** 으로 남기고 넘어갑니다.
- **한 회차짜리 지시를 상시 규칙으로 박지 마세요.**

### 받자마자 기존 코드를 일괄로 맞추지 마세요

🚩 새 규칙을 받았다고 저장소 전체를 고치지 않습니다. **지금부터의 변경**에만
적용하고, 기존 위반은 세어서 **BASELINE 으로 못박고 새 위반만 실패**하게
합니다(래칫). 예: `scripts/validate/contrast_baseline.json` 의 대비 미달 11건.

즉시 고칠 예외는 셋뿐입니다: **① 데이터 손실 ② 저장소에 들어간 비밀·키
③ 사용자가 지금 겪는 결함.**

---

## 어디에 무엇이 있나

| 문서 | 내용 |
| --- | --- |
| `docs/data-sources.md` | 원천별 상태와 이용 근거, 차단 어댑터를 켜는 절차 |
| `docs/data-permission-emails.md` | 어느 기관에 무엇을 요청하는가, 메일 초안 |
| `docs/architecture.md` | 구조와 테스트 배치 |
| `docs/design-system.md` | 토큰, 밀도, 화면 크기 검사 |
| `docs/information-architecture.md` | 탭과 화면 배치의 근거 |
| `docs/full-audit-2026-08-31.md` | 전체검수 기록 (F-1~F-9) |
| `docs/fix-backlog.md` | 결함 이력 |
| `docs/privacy-policy.md` | 개인정보처리방침 (공개 URL) |
| `docs/release-checklist.md` | 출시 전 확인 |

**지난 날짜가 붙은 기록 문서는 소급 수정하지 마세요.** 지금의 진실은 살아 있는
문서 한 곳이 말합니다.

---

## 아직 안 된 것 (미확인으로 남긴 것 포함)

- **실기기 탭 가능성 미확인** — 스텁 테스트는 "레이어가 겹쳐 버튼이 안 눌리는"
  종류를 원리적으로 못 봅니다
- 동기화 실패를 사용자에게 알리는 표시가 어느 화면에도 없음 (F-6, 미룸 결정)
- 색 대비 미달 11건 (베이스라인 고정)
- 골든 노후 감지 없음
- 서명 키 미생성 — 만들면 **오프라인 두 곳에 백업**해야 합니다
